{
  "name": "greywrought-clause",
  "private": true,
  "type": "module",
  "packageManager": "bun@1.3.13",
  "imports": {
    "#clause-runtime-wasm": "./build/host/wasm/clause_runtime.js"
  },
  "scripts": {
    "verify:clause": "bun scripts/verify-clause-pin.ts",
    "build:resident": "cargo build --release --bin resident_generation --locked",
    "stage:clause": "bun run verify:clause && bun scripts/stage-clause.ts",
    "typecheck:host": "bun run stage:clause && bun ./node_modules/typescript/bin/tsc --noEmit",
    "build:host": "bun run typecheck:host && bun build src/host/public-url.ts --no-bundle --outfile=build/host/public-url.js && bun build src/host/play.ts --outfile=build/host/play.js --external=three --external='#clause-runtime-wasm' && bun build src/host/resident-worker.ts --outfile=build/host/resident-worker.js && bun build src/host/play-server.ts --no-bundle --outfile=build/host/play-server.js",
    "test:rts-journey": "bun acceptance/browser/rts-journey.ts",
    "build:play": "bun run build:resident && bun run build:host",
    "build:static": "bun run build:play && cargo run --bin materialize --locked && bun scripts/build-static.ts && bun scripts/release-budget.ts",
    "check:release": "bun run verify:clause && bun run build:static",
    "play": "bun run build:play && bun build/host/play-server.js"
  },
  "dependencies": {
    "three": "^0.185.1"
  },
  "devDependencies": {
    "@types/bun": "1.3.13",
    "@types/three": "^0.185.4",
    "typescript": "^5.9.2"
  }
}
