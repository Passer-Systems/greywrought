# Workshop consumer evidence, 2026-09-06

The Clause world in `greywrought:src/world/workshop-expedition.clause` owns six
component occurrences, their actual upstream connections, positive power
closure, finite capacity totals, pilot decisions, expedition progress, damage,
salvage and repair. The native host transports input, projections and opaque
checkpoints. This is a bounded workshop experience, not evidence of a finished
game or a moat.

The immutable compiler is `f96aa414b880c15bfdf79f40f503b2fca08d2636`.
`check-source` opened the complete world in 177.817 ms. The focused native
consumer file passed both tests in 46.53 seconds after a 22.83-second incremental
build. It uses the actual resident Clause workbench and native 16 ms ticks.

| Preparation | Ticks until return | Cooling ticks | Maximum heat | Lance health on return | Stock on return |
| --- | ---: | ---: | ---: | ---: | ---: |
| Base machine, cautious pilot | 2185 | 637 | 35.20 | 7.47 | at least 27 |
| Same machine, assault pilot | 1833 | 285 | 80.22 | 14.73 | at least 27 |
| Assault, auxiliary source mounted | 1770 | not separately recorded | 58.35 | greater than disconnected-cooling case | 27.00 |
| Same auxiliary build/pilot, cooling link severed | 6041 | greater than connected-cooling case | 80.22 | less than connected-cooling case | 15.00 |

The auxiliary source increases output from 10 to 16 and component mass from 24
to 32. The observed departure slows, while the added output admits firing and
cooling together. Doctrine comparison holds the build fixed. Cooling comparison
holds the auxiliary build and doctrine fixed. After the first actual return,
repair spends banked stock, mounting armor slows departure, and the same
resident machine relaunches with its remaining consequences intact.

The other focused checks observed downstream power withdrawal after severing
the drive supply, lance recovery when wired directly to the core, absence of
function from a destroyed lance, and repair from zero to 45 health costing
11.25 stock. Native opaque save/reopen retains the accepted state and edited
source. An active salvage fixture changes the offered rate expression from 3
to 6: accumulated cargo is retained and the next actual 16 ms increment changes
from 0.048 to 0.096. Returned salvage and wear also survive a checkpoint.

## Authoring cost and limits

This lane took approximately 29 minutes from admission to passing consumer
checks, including waiting for upstream compiler repairs; active work and wait
time were not separately instrumented. Initial Rust test compilation took
40.30 seconds. No functional test failed after the repaired source opened.

Five rejected source-check attempts exposed three compiler capabilities:
guarded optional Boolean power derivation; typed Text inequality in phase
guards; and finite aggregates producing a shared structured derived value.
Their executable specimens remain under `greywrought:acceptance/language/` as
`workshop-powered.clause`, `workshop-phase-guard.clause` and
`workshop-capacity.clause`. The compiler owner repaired these capabilities
upstream; the consumer contains no fallback implementation.

The authoring surface currently has 14 Component role contracts (13 stored
properties and derived power), 21 Workshop role contracts (20 stored
properties and derived capacity), three Doctrine role contracts, one
eight-field Capacity shape, five laws, eleven handlers and eight physical
input bindings. Each additional component in this existing family specifies
13 ordinary property facts; the auxiliary generator required no new handler.
Power roots/links and aggregate capacity each have one authored definition.
Both displayed capacities and simulation consume that shared derived result.
The pilot's report consumes its actual selected action.

The measured ordinary live edit changes one source effect expression at one
authoritative location. The declaration, input contract, save adapter and
projection adapter do not change. Tests repeat the offered expression as an
exact operation selector and carry numerical expectations; they do not execute
a second gameplay model. Initial telemetry seeds, role/shape declarations,
record patterns and explicit withdraw/include replacements remain authoring
overhead. This tranche does not establish a comparative authoring-speed claim.

The tests contain six components, not the required 100-actor workload. They
make no claim about that performance gate, the 250 ms editing requirement,
native rendering, edit-to-visible latency or the player's experience of the
scene. Those remain the parent's integration and measurement boundaries.

Reproduction from an owned checkout: use the approved capacity launcher with
`nix develop ./vendor/clause -c cargo test --test workshop_expedition
--target-dir build/workshop-target -- --nocapture --test-threads=1`.
